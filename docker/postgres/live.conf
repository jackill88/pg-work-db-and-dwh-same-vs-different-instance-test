# Dedicated live OLTP PostgreSQL (separate-instances scenario).

include '/etc/postgresql/bench/common.conf'

shared_buffers = 384MB
effective_cache_size = 3GB
work_mem = 4MB
maintenance_work_mem = 256MB
max_connections = 220
