# Single PostgreSQL server hosting both live and dwh databases.
# Tuned for ~16 GB RAM host with one shared PG process.

include '/etc/postgresql/bench/common.conf'

shared_buffers = 512MB
effective_cache_size = 4GB
work_mem = 4MB
maintenance_work_mem = 256MB
max_connections = 250
