# Dedicated DWH PostgreSQL (separate-instances scenario).
# Higher work_mem for analytical sorts; lower max_connections.

include '/etc/postgresql/bench/common.conf'

shared_buffers = 512MB
effective_cache_size = 3GB
work_mem = 16MB
maintenance_work_mem = 512MB
max_connections = 64
